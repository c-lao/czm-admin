// export const buildProp = (prop,key?:string) =>{

// }

// export const buildProps = (props) => {
//   Object.entries(props).forEach(([key,value]) => [
//     key,
//     buildProp(value as any, key),
//   ])
// }
